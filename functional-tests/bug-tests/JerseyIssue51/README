Tests the fix for issue https://jersey.dev.java.net/issues/show_bug.cgi?id=51, i.e., tests that UriBuilder.fromUri(String).build() works with all scheme types like http, https, ftp, mailto, news, urn.
